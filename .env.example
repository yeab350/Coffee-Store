# MongoDB connection string (Atlas or local)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# Database name (optional)
MONGODB_DB=abyssinia_beans

# JWT secret used to sign login tokens (set to a long random string)
JWT_SECRET=change-me-to-a-long-random-string

# API port (optional)
PORT=5175

# Primary admin email (optional)
PRIMARY_ADMIN_EMAIL=yeabtsegaye350@gmail.com

# OpenAI API key (required for the public chatbot)
OPENAI_API_KEY=sk-your-key

# OpenAI model (optional)
OPENAI_MODEL=gpt-4.1-mini

# Frontend can point to the API (Vite reads variables prefixed with VITE_*)
VITE_API_BASE_URL=http://127.0.0.1:5175

Drop coffee images here (JPG/PNG/WEBP).

How it works:
- Each coffee card tries to load: /coffees/<coffee-id>.jpg
- Example: /coffees/bona-zuira-natural.jpg

If an image is missing, the UI automatically falls back to /coffee-placeholder.svg.
